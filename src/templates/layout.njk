<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Nawabi Kitchens{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root { --emerald: #023020; --rose-gold: #D4AF37; --soft-ivory: #FCF9F2; }
        body { background-color: var(--soft-ivory); color: var(--emerald); font-family: 'Georgia', serif; }
        .navbar { background: var(--emerald) !important; border-bottom: 2px solid var(--rose-gold); }
        .nav-link { color: var(--rose-gold) !important; text-transform: uppercase; letter-spacing: 1px; }
        .nav-link.active { font-weight: bold; }
    </style>
    {% block styles %}{% endblock %}
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark sticky-top">
        <div class="container">
            <a class="navbar-brand fw-bold" href="index.html">{% block brand %}Nawabi Kitchens{% endblock %}</a>
            <!-- Note: simplified nav toggle for mobile not fully implemented in original cleanly, keeping structure -->
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link {% if page == 'home' %}active{% endif %}" href="index.html">Home</a></li>
                    <li class="nav-item"><a class="nav-link {% if page == 'about' %}active{% endif %}" href="about.html">About</a></li>
                    <li class="nav-item"><a class="nav-link {% if page == 'contact' %}active{% endif %}" href="contact.html">Contact</a></li>
                </ul>
            </div>
        </div>
    </nav>

    {% block content %}{% endblock %}

    {% block footer %}
    <footer class="py-5 text-center mt-5" style="background: var(--emerald); color: var(--rose-gold);">
        <p class="small text-uppercase mb-0">Nawabi Kitchens © 2025</p>
    </footer>
    {% endblock %}
</body>
</html>
